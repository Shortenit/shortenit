# Production overrides — pulls pre-built images from Docker Hub
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  frontend:
    image: shortenit/frontend:latest

  backend:
    image: shortenit/backend:latest
